from django.urls import path
from . import views
from django.conf import settings
from django.conf.urls.static import static


urlpatterns = [
#     # path('home', views.home, name="home"),
#     # path('request/<user_id>', views.inviterequest, name="request"),
]
# + static(settings.ADVISOR_STATIC_URL, document_root=settings.ADVISOR_STATIC_ROOT)
